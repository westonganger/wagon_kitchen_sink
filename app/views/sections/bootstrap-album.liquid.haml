---
{
  "name": "Bootstrap Album",
  "icon": "default",
  "keep_icon": true,
  "keep_label": true,
  "settings": [
  ],
  "blocks": [
  ],
  "default": {
    "settings": {},
    "blocks": [
    ]
  },
  "dropzone_presets": [
    {
      "name": "Bootstrap Album",
      "category": "Bootstrap",
      "settings": {},
      "blocks": [
      ]
    }
  ]
}

---

{% assign default_image = 'samples/cat.png' | theme_image_url %}

.album.py-5.bg-light
  .container
    .row
      {% for product in contents.products %}
      .col-md-4
        .card.mb-4.shadow-sm
          %img.bd-placeholder-img.card-img-top{:height => "225", :width => "100%", src: "{{ product.image.url | default: default_image }}"}
          .card-body
            %p.card-text {{ product.description | default: 'A description about the item'  }}
            .d-flex.justify-content-between.align-items-center
              .btn-group
                %button.btn.btn-sm.btn-outline-secondary{:type => "button"} {{ section.settings.primary_link_text | default: 'View' }}
                %button.btn.btn-sm.btn-outline-secondary{:type => "button"} {{ section.settings.secondary_link_text | default: 'Buy Now' }}
              %small.text-muted {{ product.price | money | default: today }}
      {% endfor %}
